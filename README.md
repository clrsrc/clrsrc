# clrsrc

**clrsrc** is a strong, original UCI chess engine written from scratch in Rust.
It uses a self-trained NNUE (efficiently-updatable neural network) for
evaluation, a modern alpha-beta search with the usual array of pruning and
reduction heuristics, and Lazy SMP for multi-threaded search. Its neural
network was trained entirely on self-play data generated by a home-built
distributed cluster of laptops and smartphones.

> **License:** GNU GPL-3.0-or-later. See [`LICENSE`](LICENSE) and
> [`CREDITS.md`](CREDITS.md).

---

## Highlights

- **Original Rust engine** — bitboard board representation, magic-bitboard move
  generation, fully legal move generation, Zobrist hashing.
- **Self-trained NNUE evaluation** — a `768 → 768` perspective network with
  4 king-input buckets and SCReLU activation, trained with
  [bullet](https://github.com/jw1912/bullet) on ~200M self-play positions that
  were re-labelled ("rescored") with Stockfish evaluations.
- **Modern search** — iterative deepening, principal variation search,
  transposition table, null-move pruning, late move reductions, futility &
  reverse-futility pruning, SEE-based move ordering, history / continuation /
  capture history heuristics, singular extensions, correction history.
- **Lazy SMP** — near-linear scaling measured at ~6.7× on 7 threads.
- **Syzygy tablebases** — endgame probing via
  [shakmaty-syzygy](https://github.com/niklasf/shakmaty-syzygy).
- **Polyglot opening book** support (optional).
- **Built-in self-play data generation** (`datagen`) and Stockfish-based
  rescoring (`rescore`) — the full pipeline used to train the network ships
  with the engine.

---

## Downloads

The [Releases](../../releases) page provides:

| Artifact | Description |
|----------|-------------|
| `clrsrc.exe` | Windows x86-64 binary (build it yourself for best performance — see below) |
| `clrsrc_v32_seed_b.nnue` | The trained evaluation network |
| `jugernaut_v4.book` | Opening / experience book in JBK2 format (optional — see below) |

Place the `.nnue` file next to the executable (it is the default `EvalFile`),
or point the `EvalFile` UCI option at it.

**Opening book.** clrsrc now ships an opening/experience book,
`jugernaut_v4.book` (~192k positions, JBK2 v2 format). It was generated entirely
from self-play and is freely redistributable under this project's license. To
use it, point the `ExpFile` UCI option at the file and set `PlayFromExp true`
(see the options table below). It is **optional** and **off by default** — the
engine's strength figures and `bench` do not depend on it.

clrsrc also still supports any standard Polyglot `.bin` book via the legacy
`OwnBook` / `BookFile` options.

---

## Building from source

```bash
git clone <repo-url>
cd clrsrc
cargo build --release
# binary at target/release/clrsrc
```

A native build (`.cargo/config.toml` sets `target-cpu=native`) is recommended —
it lets the compiler use the best available SIMD (AVX2/AVX-512) for the NNUE
inner loop, which is a large NPS win over a generic build.

Requirements: a recent stable Rust toolchain (see [rustup.rs](https://rustup.rs)).

---

## Usage

clrsrc speaks [UCI](https://en.wikipedia.org/wiki/Universal_Chess_Interface),
so it works with any UCI GUI (Cute Chess, Arena, BanksiaGUI, en-croissant, …).

Point your GUI at the `clrsrc` executable and set `EvalFile` to the `.nnue`
file. From the command line:

```
$ ./clrsrc
uci
id name clrsrc 1.0.1
...
uciok
```

### UCI options

| Option | Default | Meaning |
|--------|---------|---------|
| `Hash` | 64 | Transposition-table size in MB |
| `Threads` | 1 | Search threads (Lazy SMP) |
| `EvalFile` | — | Path to the NNUE file |
| `OwnBook` | false | Use a Polyglot opening book |
| `BookFile` | — | Path to the Polyglot `.bin` book |
| `BestBookMove` | true | Pick the highest-weighted book move (vs. weighted-random) |
| `ExpFile` | — | Path to a JBK2 experience/opening book (e.g. `jugernaut_v4.book`) |
| `PlayFromExp` | false | Play book moves from the `ExpFile` |
| `BookVariety` | strict | Book-move tolerance: `strict` / `±15cp` / `±30cp`, with a WDL filter |
| `LearnDuringPlay` | false | Append the engine's deep root judgments to `<ExpFile>.overlay` |
| `ExpMinSaveDepth` | 16 | Minimum depth for a position to be written when learning |
| `SyzygyPath` | — | Directory with Syzygy tablebase files |
| `SyzygyProbeDepth` | 1 | Minimum depth for TB probing |
| `SyzygyProbeLimit` | 6 | Max pieces to probe |
| `Ponder` | true | Allow pondering |
| `LongTimeNMP` | false | Long-time-control null-move tweak |

### Other commands

```bash
clrsrc bench [depth] [nnue]      # fixed-position node benchmark
clrsrc perft <depth> [fen]       # move-generation perft
clrsrc datagen <games> <soft_nodes> <out.bin> <threads> [book.bin]
clrsrc rescore <args>            # relabel training data with Stockfish
clrsrc exp <file.book> [fen]     # inspect a JBK2 book: entries + selected move
clrsrc expmerge <book> <overlay> <out> [--clrsrc-mirror]   # consolidate a learned overlay
```

---

## The training pipeline

clrsrc's network was not trained on anyone else's data. Every position came
from clrsrc's own self-play, generated on a heterogeneous home cluster — a
desktop, several old laptops booted from Debian Live-USB sticks, and a handful
of Android phones running Termux — all orchestrated over SSH.

The full story (machine fleet, SSH orchestration, the SETI-style work-unit
pool, rescoring, and training) is documented in [`CLUSTER.md`](CLUSTER.md).

---

## Acknowledgements

### Claude Code

clrsrc was built, debugged, optimized, and operated hand-in-hand with
**[Claude Code](https://www.anthropic.com/claude-code)**, Anthropic's agentic
coding assistant. Its contribution to this project was substantial and
genuinely load-bearing — not a footnote. Across many months it:

- implemented and refactored large parts of the engine (search heuristics, the
  NNUE loader and SIMD evaluation, time management, move generation, the
  datagen and rescoring pipelines);
- found and fixed subtle bugs (multi-threaded stop-flag races, a history-table
  off-by-one, pawn-underflow in move validation, NNUE bucket-encoding
  mismatches, and many more);
- designed and ran the entire distributed self-play cluster — deploying and
  compiling the engine across Windows, Linux and Android, starting and
  monitoring datagen on every device, diagnosing per-architecture performance
  and thermal issues, and managing the work-unit pool;
- drove the NNUE training and the rigorous SPRT/gauntlet testing regime that
  decided which changes were kept and which were reverted;
- maintained cross-session memory of the whole project — the machine fleet, the
  tuning results, what worked and what didn't.

Managing a multi-day, multi-device, multi-OS engine-development and
machine-learning effort as a solo developer simply would not have been
practical without it. **clrsrc would not have reached this point without
Claude Code.**

### Open-source chess community

clrsrc stands on the shoulders of the open-source chess-programming community.
It ports code and ideas from **Stockfish**, **Stash**, **Viridithas** and
**Reckless**, trains its network with **bullet**, and links the **shakmaty** /
**shakmaty-syzygy** libraries. Every source we drew from is credited explicitly
in [`CREDITS.md`](CREDITS.md) — please read it.

---

## License

Copyright (C) 2026 clrsrc contributors.

This program is free software: you can redistribute it and/or modify it under
the terms of the GNU General Public License as published by the Free Software
Foundation, either version 3 of the License, or (at your option) any later
version.

This program is distributed in the hope that it will be useful, but WITHOUT ANY
WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A
PARTICULAR PURPOSE. See the GNU General Public License for more details.

See [`LICENSE`](LICENSE) for the full text.
